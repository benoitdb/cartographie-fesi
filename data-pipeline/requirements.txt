pandas>=2.0.0
# 3.1.0, pas 3.10.0 : openpyxl n'a jamais dépassé 3.1.x, et `>=3.10.0` est une
# contrainte insatisfiable qui faisait échouer toute installation depuis zéro.
openpyxl>=3.1.0
rapidfuzz>=3.0.0
# Format binaire .xls (pas .xlsx) : source hors-Synergie du PON FSE 2014-2020
# (issue #68), publiée par fse.gouv.fr dans ce format hérité. openpyxl ne le lit
# pas.
xlrd>=2.0.1
