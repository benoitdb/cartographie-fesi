# Dépendances du spike dbt (issue #135) — VOLONTAIREMENT séparées de
# dashboard/requirements.txt : dbt ne doit jamais partir sur Streamlit Cloud,
# qui ne régénère aucune donnée (les Parquet sont committés, cf. CLAUDE.md).
dbt-core~=1.9
dbt-duckdb~=1.9
dbt-postgres~=1.9
# Recoupement contre les vues PostgreSQL pendant la période de recouvrement
# (verifier_vues_postgres.py). Pas nécessaire au job CI, qui ne cible que DuckDB.
psycopg2-binary~=2.9
